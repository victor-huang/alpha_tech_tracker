# Code Review — 2026-04-27 to 2026-04-29 Commits

Adversarial review of code-changing commits from Apr 27–29. Pure docs, chore, and
test-only commits are skipped.

---

## Commits Reviewed

| Date | SHA | Summary |
|---|---|---|
| 4/27 | `5bf447d` | fix: always use SIP feed for historical warmup bar fetches |
| 4/27 | `da3810e` | fix: retry PRE-CLOSE SYNC once before concluding manual close |
| 4/27 | `c021a2b` | fix: increase PRE-CLOSE SYNC to 3 attempts with 3s delay each |
| 4/27 | `2baed52` | fix: use entry order status to distinguish broker API lag from manual close |
| 4/27 | `c1ab30d` | fix: handle TS positions API display format in _ts_to_occ |
| 4/27 | `3fed6eb` | fix: assign correct rank to fast-path signals after collection deadline |
| 4/27 | `c614e4a` | fix: tighten stock FILL_ESC step1 policy to reduce exit slippage |
| 4/29 | `cd50fe5` | fix: accumulate closed_contracts additively across tranche fills (G31) |
| 4/29 | `519f342` | fix: set exit_fill_price from manual broker close in _close_stock_position (G37) |
| 4/29 | `24ba321` | fix: cancel BRE/reversal watchers for DD stopout tickers (G38) |
| 4/29 | `efde1d0` | fix: skip sequential window entries when budget is zero or negative (G35) |
| 4/29 | `cb1a6c3` | fix: guard ask=0 stale quote in _stock_bid_ask (G36) |
| 4/29 | `76fb295` | fix: use _effective_contracts in print_status closed-position qty (G34) |
| 4/29 | `5ef9095` | fix: add APP to _NON_PENNY_PILOT_TICKERS |

---

## CONFIRMED BUGS

None found. See note below on one pre-existing gap.

---

## NOTED (ALREADY FIXED DOWNSTREAM)

### NOTE-1 — Missing `close_order_failed = False` in stock manual-close success path

`519f342` added `exit_fill_price` assignment from broker order history when a stock
position is found manually closed. However at that point the success branch did not
reset `pos.close_order_failed = False`, leaving a stale True value that would trigger
unnecessary reconciliation retries.

**Status: fixed by `681a6bc` (2026-05-01).** That commit restructured the same block
and added the explicit reset. Current code at `position_monitor.py:612` is correct.

---

## DISMISSED FINDINGS

### PRE-CLOSE SYNC series coherence (da3810e → c021a2b → 2baed52)

The three commits form a coherent progression:
- `da3810e`: adds 1 retry with 3s sleep
- `c021a2b`: expands to 3 total attempts
- `2baed52`: replaces the retry loop with a deterministic entry-order-status check

The later commit's approach (entry order check) is strictly better — it's not a
retry-based heuristic but a definitive broker confirmation. No residual retry logic
from earlier commits remains. Clean.

### TS symbol format conversion (c1ab30d)

Regex handles both zero-padded OSI (`AAPL241220C00230000`) and TradeStation display
format (`AAPL 241220C00230`). Strike quantization to 8-digit integer padding is
correct. Tests cover whole-dollar, fractional, and large strikes for both C and P.
No issues.

### Fast-path rank assignment (3fed6eb)

Captures `open_position_count` before incrementing → rank=0 for first entry,
rank=1 for second. Matches the normal collection-path rank assignment logic. Correct.

### closed_contracts accumulation (cd50fe5)

Removed the `pos.closed_contracts = 0` reset at the top of `_close_option_position`,
switched to `pos.closed_contracts += filled`. Field defaults to 0 in the dataclass,
so first call starts accumulation from 0. Correct.

### Zero/negative sequential budget guard (efde1d0)

Guards `window_budget is not None and window_budget <= _D("0")` before entering
any positions. Returns early. Tests confirm zero, negative, and None all skip. Correct.

### ask=0 stale quote guard (cb1a6c3)

`if ask == 0: ask = bid` prevents mid from computing as `bid / 2`. Simple and safe.
Correct.

### DD watcher cancellation scope (24ba321)

Cancels re-entry watchers for DD stopout tickers by `(ticker, primary_exit_bar_time)`
key — not just by ticker — so watchers in other windows for the same ticker are not
inadvertently cancelled. Scoped correctly.

---

## OVERALL ASSESSMENT

All Apr 27–29 code changes are clean. The one gap found (Note-1) was already patched
by a later commit. No new issues require action.
